// Shared inline style objects used across Workbench sub-components.
// Kept minimal to preserve the mechanical nature of the split.

export const fieldHintStyle = {
  fontSize: 10,
  opacity: 0.5,
  marginTop: 4,
}

export const flexRowGapStyle = {
  display: "flex",
  alignItems: "center",
  gap: 8,
}
